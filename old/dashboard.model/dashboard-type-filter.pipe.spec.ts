import { DashboardTypeFilterPipe } from './dashboard-type-filter.pipe';

describe('DashboardTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DashboardTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
